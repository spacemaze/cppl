// This is a generated file. Don't edit it.
// Edit main.cppl.in and use bash.sh or test-all.sh
// to generate it again.
// ------------------------------------------------

// RUN:  %clang -cc1 -std=c++17 -xc++ -levitation-build-preamble %S/../preamble.hpp -o %T/preamble.pch
// Building declaration 'Inputs/A'...
// RUN:  %clang -cc1 -std=c++17 -levitation-preamble=%T/preamble.pch -xc++ -flevitation-build-decl -emit-pch %S/Inputs/A.cppl -levitation-decl-ast-meta=%T/Inputs_A.decl-ast-meta -o %T/Inputs_A.decl-ast
// Building declaration 'Inputs/B'...
// RUN:  %clang -cc1 -std=c++17 -levitation-preamble=%T/preamble.pch -xc++ -flevitation-build-decl -emit-pch %S/Inputs/B.cppl -levitation-decl-ast-meta=%T/Inputs_B.decl-ast-meta -o %T/Inputs_B.decl-ast
// Building object 'Inputs/A'...
// RUN:  %clang -cc1 -std=c++17 -levitation-preamble=%T/preamble.pch -xc++ -flevitation-build-object -emit-obj %S/Inputs/A.cppl -o %T/Inputs_A.o
// Building object 'Inputs/B'...
// RUN:  %clang -cc1 -std=c++17 -levitation-preamble=%T/preamble.pch -xc++ -flevitation-build-object -emit-obj %S/Inputs/B.cppl -o %T/Inputs_B.o
// Building object 'main'...
// RUN:  %clang -cc1 -std=c++17 -levitation-preamble=%T/preamble.pch -xc++ -flevitation-build-object -levitation-dependency=%T/Inputs_A.decl-ast -levitation-dependency=%T/Inputs_B.decl-ast -emit-obj %S/main.cppl -o %T/main.o
// RUN:  %clangxx %T/main.o %T/Inputs_A.o %T/Inputs_B.o -o %T/app.out
// RUN:  %T/app.out
#import Inputs::A
#import Inputs::B

int main() {
  with (
    auto TestScope = levitation::Test::context()
        .expect(123)
        .expect(777)
    .open()
  ) {
    levitation::Test::context()
        << V::get()
        << C::get();
  }
  return levitation::Test::result();
}

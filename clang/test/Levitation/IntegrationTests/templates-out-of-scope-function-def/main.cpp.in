int main() {
  with (
    auto TestScope = levitation::Test::context()
        .expect("P1::A::A()")
        .expect("P1::A::f()")
        .expect("P1::A::operator<<(int)")
        .expect("P1::A::~A()")
    .open()
  ) {
    P1::A<int> a;
    a.f();
    a << 1;
  }
  return levitation::Test::result();
}

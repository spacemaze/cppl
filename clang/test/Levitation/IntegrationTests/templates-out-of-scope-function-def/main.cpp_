namespace M {
  template<typename T>
  struct A {
    static void f();
  };

  template<typename T>
  void A<T>::f() {}
}

int main() {
// CHECK: P1::A::A()
// CHECK: P1::A::f()
// CHECK: P1::A::operator<<(int)
// CHECK: P1::A::~A()

  P1::A<int> a;

  a.f();
  
  a << 1;

  return 0;
}

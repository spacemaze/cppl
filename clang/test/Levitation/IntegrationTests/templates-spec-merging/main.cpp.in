int main() {
  P1::D::f();

  with (
    auto TestScope = levitation::Test::context()
        .expect("P1::D::f()")
        .expect("P1::B::f()")
        .expect("P1::A::f()")
        .expect("P1::C::f()")
        .expect("P1::A::f()")
    .open()
  ) {
    P1::D::f();
  }
  return levitation::Test::result();

}

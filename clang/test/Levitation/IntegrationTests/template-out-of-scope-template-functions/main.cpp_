namespace M {
  template<typename T>
  struct A {
    static void f();
  };

  template<typename T>
  void A<T>::f() {}
}

int main() {
  // CHECK: P1::A<T>::f<U>()
  // CHECK: P1::A<T>::operator<<(U)
  
  P1::A<int> a;
  a.f<int>();
  a << 1;

  return 0;
}

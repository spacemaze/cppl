int main() {
  with (
    auto TestScope = levitation::Test::context()
        .expect("P1::F::f()")
        .expect("P1::B::ff()")
        .expect("P1::A::f()")
        .expect("P1::E::f()")
        .expect("P1::C::f()")
    .open()
  ) {
    P1::F::f();
  }
  return levitation::Test::result();
}
